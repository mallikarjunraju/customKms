kind: EncryptionConfiguration
apiVersion: apiserver.config.k8s.io/v1
resources:
  - resources:
      - secrets
    providers:
      - kms:
          name: customkmsprovider
          endpoint: unix:///opt/customkms.socket
          cachesize: 1000
      - identity: {}
